export enum HelpSidebarKeys {
  RECON = "recon",
}

export enum ReconResultStatus {
  SUCCESS = "SUCCESS",
  FAILURE = "FAILURE",
  PENDING = "PENDING",
}

export enum StatementType {
  INTERNAL = "INTERNAL",
  PROVIDER = "PROVIDER",
}
